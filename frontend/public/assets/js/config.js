/**
 * Frontend App Configuration
 * Sets the API base URL so all fetch calls reach the backend.
 * Change API_BASE if your backend runs on a different port/host.
 */
window.APP_CONFIG = {
    apiBase: 'http://localhost:3000/api/v1',
};
